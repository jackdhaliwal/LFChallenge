import java.util.List;

public interface DifferentListSorting {
	//takes list in and sorts in ascending order							
	 List<Integer> simpleSort(List<Integer> a);
	 
	//takes list in and prints to stdout 
	 void printSortedFrequency(List<Integer> b);	
	 
	//takes list in and returns # of unique val appears
	 int numberOfUniqueValues(List<Integer> c);	

}
